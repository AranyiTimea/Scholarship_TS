Task: Create a program that randomly generates integer numbers between X and Y.
o X and Y will be inserted from the keyboard.
o The user needs to guess the generated number.
o When the number is guessed, the sequence continues by generating new values.
o The program should contain also an exit code.